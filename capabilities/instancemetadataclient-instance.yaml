naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure InstanceMetadataClient — Instance
  description: 'Microsoft Azure InstanceMetadataClient — Instance. 1 operations. Lead operation: Microsoft Azure Get Instance.
    Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Instance
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: instancemetadataclient-instance
    baseUri: http://169.254.169.254/metadata
    description: Microsoft Azure InstanceMetadataClient — Instance business capability. Self-contained, no shared references.
    resources:
    - name: instance
      path: /instance
      operations:
      - name: microsoftazureinstancesgetmetadata
        method: GET
        description: Microsoft Azure Get Instance
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: instancemetadataclient-instance-rest
    port: 8080
    description: REST adapter for Microsoft Azure InstanceMetadataClient — Instance. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/instance
      name: instance
      description: REST surface for instance.
      operations:
      - method: GET
        name: microsoftazureinstancesgetmetadata
        description: Microsoft Azure Get Instance
        call: instancemetadataclient-instance.microsoftazureinstancesgetmetadata
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: instancemetadataclient-instance-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure InstanceMetadataClient — Instance. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-get-instance
      description: Microsoft Azure Get Instance
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: instancemetadataclient-instance.microsoftazureinstancesgetmetadata
      outputParameters:
      - type: object
        mapping: $.
