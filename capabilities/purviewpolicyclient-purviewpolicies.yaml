naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure PurviewPolicyClient — PurviewPolicies
  description: 'Microsoft Azure PurviewPolicyClient — PurviewPolicies. 1 operations. Lead operation: Microsoft Azure Api To
    List The Purview Rbac Policies Based On A Scope. Self-contained Naftiko capability covering one Microsoft Azure business
    surface.'
  tags:
  - Microsoft Azure
  - PurviewPolicies
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: purviewpolicyclient-purviewpolicies
    baseUri: https://management.azure.com
    description: Microsoft Azure PurviewPolicyClient — PurviewPolicies business capability. Self-contained, no shared references.
    resources:
    - name: scope-providers-Microsoft.Purview-policies
      path: /{scope}/providers/Microsoft.Purview/policies
      operations:
      - name: microsoftazurepurviewpolicieslist
        method: GET
        description: Microsoft Azure Api To List The Purview Rbac Policies Based On A Scope
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $filter
          in: query
          type: string
          description: 'Supported filters : $filter=policyType eq SelfService, $filter=policyType eq SqlDevops. Returns only
            the policies of the specified type. If not specified, all p'
  exposes:
  - type: rest
    namespace: purviewpolicyclient-purviewpolicies-rest
    port: 8080
    description: REST adapter for Microsoft Azure PurviewPolicyClient — PurviewPolicies. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/{scope}/providers/microsoft-purview/policies
      name: scope-providers-microsoft-purview-policies
      description: REST surface for scope-providers-Microsoft.Purview-policies.
      operations:
      - method: GET
        name: microsoftazurepurviewpolicieslist
        description: Microsoft Azure Api To List The Purview Rbac Policies Based On A Scope
        call: purviewpolicyclient-purviewpolicies.microsoftazurepurviewpolicieslist
        with:
          $filter: rest.$filter
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: purviewpolicyclient-purviewpolicies-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure PurviewPolicyClient — PurviewPolicies. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-api-list-purview
      description: Microsoft Azure Api To List The Purview Rbac Policies Based On A Scope
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: purviewpolicyclient-purviewpolicies.microsoftazurepurviewpolicieslist
      with:
        $filter: tools.$filter
      outputParameters:
      - type: object
        mapping: $.
