naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure DevCenter — Environment Types
  description: 'Microsoft Azure DevCenter — Environment Types. 1 operations. Lead operation: Microsoft Azure Get Projects
    Projectname Environmenttypes. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Environment Types
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: devcenter-environment-types
    baseUri: ''
    description: Microsoft Azure DevCenter — Environment Types business capability. Self-contained, no shared references.
    resources:
    - name: projects-projectName-environmentTypes
      path: /projects/{projectName}/environmentTypes
      operations:
      - name: microsoftazureenvironmentslistenvironmenttypes
        method: GET
        description: Microsoft Azure Get Projects Projectname Environmenttypes
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: devcenter-environment-types-rest
    port: 8080
    description: REST adapter for Microsoft Azure DevCenter — Environment Types. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/projects/{projectname}/environmenttypes
      name: projects-projectname-environmenttypes
      description: REST surface for projects-projectName-environmentTypes.
      operations:
      - method: GET
        name: microsoftazureenvironmentslistenvironmenttypes
        description: Microsoft Azure Get Projects Projectname Environmenttypes
        call: devcenter-environment-types.microsoftazureenvironmentslistenvironmenttypes
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: devcenter-environment-types-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure DevCenter — Environment Types. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: microsoft-azure-get-projects-projectname
      description: Microsoft Azure Get Projects Projectname Environmenttypes
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: devcenter-environment-types.microsoftazureenvironmentslistenvironmenttypes
      outputParameters:
      - type: object
        mapping: $.
