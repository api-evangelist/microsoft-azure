naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure Azure OpenAI Service API — Images
  description: 'Microsoft Azure Azure OpenAI Service API — Images. 1 operations. Lead operation: Microsoft Azure Create Image
    Generations. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Images
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: azure-openai-service-images
    baseUri: https://{endpoint}/openai
    description: Microsoft Azure Azure OpenAI Service API — Images business capability. Self-contained, no shared references.
    resources:
    - name: deployments-deployment-id-images-generations
      path: /deployments/{deployment-id}/images/generations
      operations:
      - name: imagegenerationscreate
        method: POST
        description: Microsoft Azure Create Image Generations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_API_KEY}}'
  exposes:
  - type: rest
    namespace: azure-openai-service-images-rest
    port: 8080
    description: REST adapter for Microsoft Azure Azure OpenAI Service API — Images. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/deployments/{deployment-id}/images/generations
      name: deployments-deployment-id-images-generations
      description: REST surface for deployments-deployment-id-images-generations.
      operations:
      - method: POST
        name: imagegenerationscreate
        description: Microsoft Azure Create Image Generations
        call: azure-openai-service-images.imagegenerationscreate
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: azure-openai-service-images-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure Azure OpenAI Service API — Images. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-create-image-generations
      description: Microsoft Azure Create Image Generations
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: azure-openai-service-images.imagegenerationscreate
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
