naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure SearchServiceClient
  description: 'Microsoft Azure SearchServiceClient. 1 operations. Lead operation: Microsoft Azure Get Servicestats. Self-contained
    Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: searchserviceclient-general
    baseUri: ''
    description: Microsoft Azure SearchServiceClient business capability. Self-contained, no shared references.
    resources:
    - name: servicestats
      path: /servicestats
      operations:
      - name: microsoftazuregetservicestatistics
        method: GET
        description: Microsoft Azure Get Servicestats
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: searchserviceclient-general-rest
    port: 8080
    description: REST adapter for Microsoft Azure SearchServiceClient. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/servicestats
      name: servicestats
      description: REST surface for servicestats.
      operations:
      - method: GET
        name: microsoftazuregetservicestatistics
        description: Microsoft Azure Get Servicestats
        call: searchserviceclient-general.microsoftazuregetservicestatistics
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: searchserviceclient-general-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure SearchServiceClient. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: microsoft-azure-get-servicestats
      description: Microsoft Azure Get Servicestats
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: searchserviceclient-general.microsoftazuregetservicestatistics
      outputParameters:
      - type: object
        mapping: $.
