naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure Face Client — Operations
  description: 'Microsoft Azure Face Client — Operations. 1 operations. Lead operation: Microsoft Azure Get Operations Operationid.
    Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: face-client-operations
    baseUri: ''
    description: Microsoft Azure Face Client — Operations business capability. Self-contained, no shared references.
    resources:
    - name: operations-operationId
      path: /operations/{operationId}
      operations:
      - name: microsoftazuresnapshotgetoperationstatus
        method: GET
        description: Microsoft Azure Get Operations Operationid
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: face-client-operations-rest
    port: 8080
    description: REST adapter for Microsoft Azure Face Client — Operations. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/operations/{operationid}
      name: operations-operationid
      description: REST surface for operations-operationId.
      operations:
      - method: GET
        name: microsoftazuresnapshotgetoperationstatus
        description: Microsoft Azure Get Operations Operationid
        call: face-client-operations.microsoftazuresnapshotgetoperationstatus
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: face-client-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure Face Client — Operations. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: microsoft-azure-get-operations-operationid
      description: Microsoft Azure Get Operations Operationid
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: face-client-operations.microsoftazuresnapshotgetoperationstatus
      outputParameters:
      - type: object
        mapping: $.
