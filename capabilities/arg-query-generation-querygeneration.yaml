naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure ARG Query Generation — QueryGeneration
  description: 'Microsoft Azure ARG Query Generation — QueryGeneration. 1 operations. Lead operation: Microsoft Azure Post
    Providers Microsoft Resourcegraph Generatequery. Self-contained Naftiko capability covering one Microsoft Azure business
    surface.'
  tags:
  - Microsoft Azure
  - QueryGeneration
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: arg-query-generation-querygeneration
    baseUri: ''
    description: Microsoft Azure ARG Query Generation — QueryGeneration business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.ResourceGraph-generateQuery
      path: /providers/Microsoft.ResourceGraph/generateQuery
      operations:
      - name: microsoftazurequerygeneratequery
        method: POST
        description: Microsoft Azure Post Providers Microsoft Resourcegraph Generatequery
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: string
          required: true
  exposes:
  - type: rest
    namespace: arg-query-generation-querygeneration-rest
    port: 8080
    description: REST adapter for Microsoft Azure ARG Query Generation — QueryGeneration. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-resourcegraph/generatequery
      name: providers-microsoft-resourcegraph-generatequery
      description: REST surface for providers-Microsoft.ResourceGraph-generateQuery.
      operations:
      - method: POST
        name: microsoftazurequerygeneratequery
        description: Microsoft Azure Post Providers Microsoft Resourcegraph Generatequery
        call: arg-query-generation-querygeneration.microsoftazurequerygeneratequery
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: arg-query-generation-querygeneration-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure ARG Query Generation — QueryGeneration. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-post-providers-microsoft
      description: Microsoft Azure Post Providers Microsoft Resourcegraph Generatequery
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: arg-query-generation-querygeneration.microsoftazurequerygeneratequery
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
