naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure Qumulo.Storage — Operations
  description: 'Microsoft Azure Qumulo.Storage — Operations. 1 operations. Lead operation: Microsoft Azure Get Providers Qumulo
    Storage Operations. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: qumulostorage-operations
    baseUri: https://management.azure.com
    description: Microsoft Azure Qumulo.Storage — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Qumulo.Storage-operations
      path: /providers/Qumulo.Storage/operations
      operations:
      - name: microsoftazureoperationslist
        method: GET
        description: Microsoft Azure Get Providers Qumulo Storage Operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: qumulostorage-operations-rest
    port: 8080
    description: REST adapter for Microsoft Azure Qumulo.Storage — Operations. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/providers/qumulo-storage/operations
      name: providers-qumulo-storage-operations
      description: REST surface for providers-Qumulo.Storage-operations.
      operations:
      - method: GET
        name: microsoftazureoperationslist
        description: Microsoft Azure Get Providers Qumulo Storage Operations
        call: qumulostorage-operations.microsoftazureoperationslist
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: qumulostorage-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure Qumulo.Storage — Operations. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: microsoft-azure-get-providers-qumulo
      description: Microsoft Azure Get Providers Qumulo Storage Operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: qumulostorage-operations.microsoftazureoperationslist
      outputParameters:
      - type: object
        mapping: $.
