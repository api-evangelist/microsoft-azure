naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure Microsoft.VoiceServices.Provisioning — Numbers
  description: 'Microsoft Azure Microsoft.VoiceServices.Provisioning — Numbers. 1 operations. Lead operation: Microsoft Azure
    Get Numbers. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Numbers
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoftvoiceservicesprovisioning-numbers
    baseUri: ''
    description: Microsoft Azure Microsoft.VoiceServices.Provisioning — Numbers business capability. Self-contained, no shared
      references.
    resources:
    - name: numbers
      path: /numbers
      operations:
      - name: microsoftazurelistnumbers
        method: GET
        description: Microsoft Azure Get Numbers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: microsoftvoiceservicesprovisioning-numbers-rest
    port: 8080
    description: REST adapter for Microsoft Azure Microsoft.VoiceServices.Provisioning — Numbers. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/numbers
      name: numbers
      description: REST surface for numbers.
      operations:
      - method: GET
        name: microsoftazurelistnumbers
        description: Microsoft Azure Get Numbers
        call: microsoftvoiceservicesprovisioning-numbers.microsoftazurelistnumbers
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoftvoiceservicesprovisioning-numbers-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure Microsoft.VoiceServices.Provisioning — Numbers. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-get-numbers
      description: Microsoft Azure Get Numbers
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoftvoiceservicesprovisioning-numbers.microsoftazurelistnumbers
      outputParameters:
      - type: object
        mapping: $.
