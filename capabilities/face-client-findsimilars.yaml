naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure Face Client — Findsimilars
  description: 'Microsoft Azure Face Client — Findsimilars. 1 operations. Lead operation: Microsoft Azure Post Findsimilars.
    Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Findsimilars
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: face-client-findsimilars
    baseUri: ''
    description: Microsoft Azure Face Client — Findsimilars business capability. Self-contained, no shared references.
    resources:
    - name: findsimilars
      path: /findsimilars
      operations:
      - name: microsoftazurefacefindsimilar
        method: POST
        description: Microsoft Azure Post Findsimilars
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: string
          description: Request body for Find Similar.
          required: true
  exposes:
  - type: rest
    namespace: face-client-findsimilars-rest
    port: 8080
    description: REST adapter for Microsoft Azure Face Client — Findsimilars. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/findsimilars
      name: findsimilars
      description: REST surface for findsimilars.
      operations:
      - method: POST
        name: microsoftazurefacefindsimilar
        description: Microsoft Azure Post Findsimilars
        call: face-client-findsimilars.microsoftazurefacefindsimilar
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: face-client-findsimilars-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure Face Client — Findsimilars. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: microsoft-azure-post-findsimilars
      description: Microsoft Azure Post Findsimilars
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: face-client-findsimilars.microsoftazurefacefindsimilar
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
