naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure Face Client — Verify
  description: 'Microsoft Azure Face Client — Verify. 1 operations. Lead operation: Microsoft Azure Post Verify. Self-contained
    Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Verify
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: face-client-verify
    baseUri: ''
    description: Microsoft Azure Face Client — Verify business capability. Self-contained, no shared references.
    resources:
    - name: verify
      path: /verify
      operations:
      - name: microsoftazurefaceverifyfacetoface
        method: POST
        description: Microsoft Azure Post Verify
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: string
          description: Request body for face to face verification.
          required: true
  exposes:
  - type: rest
    namespace: face-client-verify-rest
    port: 8080
    description: REST adapter for Microsoft Azure Face Client — Verify. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/verify
      name: verify
      description: REST surface for verify.
      operations:
      - method: POST
        name: microsoftazurefaceverifyfacetoface
        description: Microsoft Azure Post Verify
        call: face-client-verify.microsoftazurefaceverifyfacetoface
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: face-client-verify-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure Face Client — Verify. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: microsoft-azure-post-verify
      description: Microsoft Azure Post Verify
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: face-client-verify.microsoftazurefaceverifyfacetoface
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
