naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure Personalizer Base — MultiSlotRank
  description: 'Microsoft Azure Personalizer Base — MultiSlotRank. 1 operations. Lead operation: Microsoft Azure Post Multi
    Slot Rank. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - MultiSlotRank
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: personalizer-base-multislotrank
    baseUri: ''
    description: Microsoft Azure Personalizer Base — MultiSlotRank business capability. Self-contained, no shared references.
    resources:
    - name: multislot-rank
      path: /multislot/rank
      operations:
      - name: microsoftazuremultislotrank
        method: POST
        description: Microsoft Azure Post Multi Slot Rank
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: string
          description: A Personalizer multi-slot Rank request.
          required: true
  exposes:
  - type: rest
    namespace: personalizer-base-multislotrank-rest
    port: 8080
    description: REST adapter for Microsoft Azure Personalizer Base — MultiSlotRank. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/multislot/rank
      name: multislot-rank
      description: REST surface for multislot-rank.
      operations:
      - method: POST
        name: microsoftazuremultislotrank
        description: Microsoft Azure Post Multi Slot Rank
        call: personalizer-base-multislotrank.microsoftazuremultislotrank
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: personalizer-base-multislotrank-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure Personalizer Base — MultiSlotRank. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-post-multi-slot
      description: Microsoft Azure Post Multi Slot Rank
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: personalizer-base-multislotrank.microsoftazuremultislotrank
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
