naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure Azure AgFoodPlatform Data Plane Service — WeatherData
  description: 'Microsoft Azure Azure AgFoodPlatform Data Plane Service — WeatherData. 1 operations. Lead operation: Microsoft
    Azure Post Weather Data :fetch. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - WeatherData
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: azure-agfoodplatform-data-plane-service-weatherdata
    baseUri: ''
    description: Microsoft Azure Azure AgFoodPlatform Data Plane Service — WeatherData business capability. Self-contained,
      no shared references.
    resources:
    - name: weather-data-:fetch
      path: /weather-data/:fetch
      operations:
      - name: microsoftazureweatherdataget
        method: POST
        description: Microsoft Azure Post Weather Data :fetch
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: api-version
          in: query
          type: string
          description: The requested API version
          required: true
        - name: weatherDataProviderRequest
          in: body
          type: string
          description: Weather data provider request.
          required: true
  exposes:
  - type: rest
    namespace: azure-agfoodplatform-data-plane-service-weatherdata-rest
    port: 8080
    description: REST adapter for Microsoft Azure Azure AgFoodPlatform Data Plane Service — WeatherData. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/weather-data/fetch
      name: weather-data-fetch
      description: REST surface for weather-data-:fetch.
      operations:
      - method: POST
        name: microsoftazureweatherdataget
        description: Microsoft Azure Post Weather Data :fetch
        call: azure-agfoodplatform-data-plane-service-weatherdata.microsoftazureweatherdataget
        with:
          api-version: rest.api-version
          weatherDataProviderRequest: rest.weatherDataProviderRequest
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: azure-agfoodplatform-data-plane-service-weatherdata-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure Azure AgFoodPlatform Data Plane Service — WeatherData. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-post-weather-data
      description: Microsoft Azure Post Weather Data :fetch
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: azure-agfoodplatform-data-plane-service-weatherdata.microsoftazureweatherdataget
      with:
        api-version: tools.api-version
        weatherDataProviderRequest: tools.weatherDataProviderRequest
      outputParameters:
      - type: object
        mapping: $.
