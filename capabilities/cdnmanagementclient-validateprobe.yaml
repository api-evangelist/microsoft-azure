naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure CdnManagementClient — ValidateProbe
  description: 'Microsoft Azure CdnManagementClient — ValidateProbe. 1 operations. Lead operation: Microsoft Azure Post Subscriptions
    Subscriptionid Providers Microsoft Cdn Validateprobe. Self-contained Naftiko capability covering one Microsoft Azure business
    surface.'
  tags:
  - Microsoft Azure
  - ValidateProbe
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: cdnmanagementclient-validateprobe
    baseUri: https://management.azure.com
    description: Microsoft Azure CdnManagementClient — ValidateProbe business capability. Self-contained, no shared references.
    resources:
    - name: subscriptions-subscriptionId-providers-Microsoft.Cdn-validateProbe
      path: /subscriptions/{subscriptionId}/providers/Microsoft.Cdn/validateProbe
      operations:
      - name: microsoftazurevalidateprobe
        method: POST
        description: Microsoft Azure Post Subscriptions Subscriptionid Providers Microsoft Cdn Validateprobe
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: validateProbeInput
          in: body
          type: string
          description: Input to check.
          required: true
  exposes:
  - type: rest
    namespace: cdnmanagementclient-validateprobe-rest
    port: 8080
    description: REST adapter for Microsoft Azure CdnManagementClient — ValidateProbe. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/subscriptions/{subscriptionid}/providers/microsoft-cdn/validateprobe
      name: subscriptions-subscriptionid-providers-microsoft-cdn-validateprobe
      description: REST surface for subscriptions-subscriptionId-providers-Microsoft.Cdn-validateProbe.
      operations:
      - method: POST
        name: microsoftazurevalidateprobe
        description: Microsoft Azure Post Subscriptions Subscriptionid Providers Microsoft Cdn Validateprobe
        call: cdnmanagementclient-validateprobe.microsoftazurevalidateprobe
        with:
          validateProbeInput: rest.validateProbeInput
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: cdnmanagementclient-validateprobe-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure CdnManagementClient — ValidateProbe. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-post-subscriptions-subscriptionid
      description: Microsoft Azure Post Subscriptions Subscriptionid Providers Microsoft Cdn Validateprobe
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: cdnmanagementclient-validateprobe.microsoftazurevalidateprobe
      with:
        validateProbeInput: tools.validateProbeInput
      outputParameters:
      - type: object
        mapping: $.
