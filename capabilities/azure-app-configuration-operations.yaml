naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure Azure App Configuration — Operations
  description: 'Microsoft Azure Azure App Configuration — Operations. 1 operations. Lead operation: Microsoft Azure Gets The
    State Of A Long Running Operation. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: azure-app-configuration-operations
    baseUri: ''
    description: Microsoft Azure Azure App Configuration — Operations business capability. Self-contained, no shared references.
    resources:
    - name: operations
      path: /operations
      operations:
      - name: microsoftazuregetoperationdetails
        method: GET
        description: Microsoft Azure Gets The State Of A Long Running Operation
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: snapshot
          in: query
          type: string
          description: Snapshot identifier for the long running operation.
          required: true
  exposes:
  - type: rest
    namespace: azure-app-configuration-operations-rest
    port: 8080
    description: REST adapter for Microsoft Azure Azure App Configuration — Operations. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/operations
      name: operations
      description: REST surface for operations.
      operations:
      - method: GET
        name: microsoftazuregetoperationdetails
        description: Microsoft Azure Gets The State Of A Long Running Operation
        call: azure-app-configuration-operations.microsoftazuregetoperationdetails
        with:
          snapshot: rest.snapshot
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: azure-app-configuration-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure Azure App Configuration — Operations. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-gets-state-long
      description: Microsoft Azure Gets The State Of A Long Running Operation
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: azure-app-configuration-operations.microsoftazuregetoperationdetails
      with:
        snapshot: tools.snapshot
      outputParameters:
      - type: object
        mapping: $.
