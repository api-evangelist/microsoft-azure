naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure ConsumptionManagementClient — Charges
  description: 'Microsoft Azure ConsumptionManagementClient — Charges. 1 operations. Lead operation: Microsoft Azure Get Scope
    Providers Microsoft Consumption Charges. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Charges
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: consumptionmanagementclient-charges
    baseUri: https://management.azure.com
    description: Microsoft Azure ConsumptionManagementClient — Charges business capability. Self-contained, no shared references.
    resources:
    - name: scope-providers-Microsoft.Consumption-charges
      path: /{scope}/providers/Microsoft.Consumption/charges
      operations:
      - name: microsoftazurechargeslistbyscope
        method: GET
        description: Microsoft Azure Get Scope Providers Microsoft Consumption Charges
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: $filter
          in: query
          type: string
          description: 'May be used to filter charges by properties/usageEnd (Utc time), properties/usageStart (Utc time).
            The filter supports ''eq'', ''lt'', ''gt'', ''le'', ''ge'', and ''and''. '
  exposes:
  - type: rest
    namespace: consumptionmanagementclient-charges-rest
    port: 8080
    description: REST adapter for Microsoft Azure ConsumptionManagementClient — Charges. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/{scope}/providers/microsoft-consumption/charges
      name: scope-providers-microsoft-consumption-charges
      description: REST surface for scope-providers-Microsoft.Consumption-charges.
      operations:
      - method: GET
        name: microsoftazurechargeslistbyscope
        description: Microsoft Azure Get Scope Providers Microsoft Consumption Charges
        call: consumptionmanagementclient-charges.microsoftazurechargeslistbyscope
        with:
          $filter: rest.$filter
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: consumptionmanagementclient-charges-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure ConsumptionManagementClient — Charges. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-get-scope-providers
      description: Microsoft Azure Get Scope Providers Microsoft Consumption Charges
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: consumptionmanagementclient-charges.microsoftazurechargeslistbyscope
      with:
        $filter: tools.$filter
      outputParameters:
      - type: object
        mapping: $.
