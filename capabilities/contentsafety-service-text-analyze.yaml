naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure ContentSafety Service — Text:analyze
  description: 'Microsoft Azure ContentSafety Service — Text:analyze. 1 operations. Lead operation: Microsoft Azure Analyze
    Text. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Text:analyze
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: contentsafety-service-text-analyze
    baseUri: ''
    description: Microsoft Azure ContentSafety Service — Text:analyze business capability. Self-contained, no shared references.
    resources:
    - name: text:analyze
      path: /text:analyze
      operations:
      - name: microsoftazuretextoperationsanalyzetext
        method: POST
        description: Microsoft Azure Analyze Text
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: string
          description: The text analysis request.
          required: true
  exposes:
  - type: rest
    namespace: contentsafety-service-text-analyze-rest
    port: 8080
    description: REST adapter for Microsoft Azure ContentSafety Service — Text:analyze. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/text-analyze
      name: text-analyze
      description: REST surface for text:analyze.
      operations:
      - method: POST
        name: microsoftazuretextoperationsanalyzetext
        description: Microsoft Azure Analyze Text
        call: contentsafety-service-text-analyze.microsoftazuretextoperationsanalyzetext
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: contentsafety-service-text-analyze-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure ContentSafety Service — Text:analyze. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-analyze-text
      description: Microsoft Azure Analyze Text
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: contentsafety-service-text-analyze.microsoftazuretextoperationsanalyzetext
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
