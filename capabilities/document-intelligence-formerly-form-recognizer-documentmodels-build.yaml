naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure Document Intelligence (formerly Form Recognizer) — documentModels:build
  description: 'Microsoft Azure Document Intelligence (formerly Form Recognizer) — documentModels:build. 1 operations. Lead
    operation: Microsoft Azure Post Documentmodels:build. Self-contained Naftiko capability covering one Microsoft Azure business
    surface.'
  tags:
  - Microsoft Azure
  - documentModels:build
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: document-intelligence-formerly-form-recognizer-documentmodels-build
    baseUri: ''
    description: Microsoft Azure Document Intelligence (formerly Form Recognizer) — documentModels:build business capability.
      Self-contained, no shared references.
    resources:
    - name: documentModels:build
      path: /documentModels:build
      operations:
      - name: microsoftazuredocumentmodelsbuildmodel
        method: POST
        description: Microsoft Azure Post Documentmodels:build
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: buildRequest
          in: body
          type: string
          description: Build request parameters.
          required: true
  exposes:
  - type: rest
    namespace: document-intelligence-formerly-form-recognizer-documentmodels-build-rest
    port: 8080
    description: REST adapter for Microsoft Azure Document Intelligence (formerly Form Recognizer) — documentModels:build.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/documentmodels-build
      name: documentmodels-build
      description: REST surface for documentModels:build.
      operations:
      - method: POST
        name: microsoftazuredocumentmodelsbuildmodel
        description: Microsoft Azure Post Documentmodels:build
        call: document-intelligence-formerly-form-recognizer-documentmodels-build.microsoftazuredocumentmodelsbuildmodel
        with:
          buildRequest: rest.buildRequest
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: document-intelligence-formerly-form-recognizer-documentmodels-build-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure Document Intelligence (formerly Form Recognizer) — documentModels:build.
      One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-post-documentmodels-build
      description: Microsoft Azure Post Documentmodels:build
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: document-intelligence-formerly-form-recognizer-documentmodels-build.microsoftazuredocumentmodelsbuildmodel
      with:
        buildRequest: tools.buildRequest
      outputParameters:
      - type: object
        mapping: $.
