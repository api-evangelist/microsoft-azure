naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure Document Intelligence (formerly Form Recognizer) — documentClassifiers:build
  description: 'Microsoft Azure Document Intelligence (formerly Form Recognizer) — documentClassifiers:build. 1 operations.
    Lead operation: Microsoft Azure Post Documentclassifiers:build. Self-contained Naftiko capability covering one Microsoft
    Azure business surface.'
  tags:
  - Microsoft Azure
  - documentClassifiers:build
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: document-intelligence-formerly-form-recognizer-documentclassifiers-build
    baseUri: ''
    description: Microsoft Azure Document Intelligence (formerly Form Recognizer) — documentClassifiers:build business capability.
      Self-contained, no shared references.
    resources:
    - name: documentClassifiers:build
      path: /documentClassifiers:build
      operations:
      - name: microsoftazuredocumentclassifiersbuildclassifier
        method: POST
        description: Microsoft Azure Post Documentclassifiers:build
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: buildRequest
          in: body
          type: string
          description: Build request parameters.
          required: true
  exposes:
  - type: rest
    namespace: document-intelligence-formerly-form-recognizer-documentclassifiers-build-rest
    port: 8080
    description: REST adapter for Microsoft Azure Document Intelligence (formerly Form Recognizer) — documentClassifiers:build.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/documentclassifiers-build
      name: documentclassifiers-build
      description: REST surface for documentClassifiers:build.
      operations:
      - method: POST
        name: microsoftazuredocumentclassifiersbuildclassifier
        description: Microsoft Azure Post Documentclassifiers:build
        call: document-intelligence-formerly-form-recognizer-documentclassifiers-build.microsoftazuredocumentclassifiersbuildclassifier
        with:
          buildRequest: rest.buildRequest
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: document-intelligence-formerly-form-recognizer-documentclassifiers-build-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure Document Intelligence (formerly Form Recognizer) — documentClassifiers:build.
      One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-post-documentclassifiers-build
      description: Microsoft Azure Post Documentclassifiers:build
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: document-intelligence-formerly-form-recognizer-documentclassifiers-build.microsoftazuredocumentclassifiersbuildclassifier
      with:
        buildRequest: tools.buildRequest
      outputParameters:
      - type: object
        mapping: $.
