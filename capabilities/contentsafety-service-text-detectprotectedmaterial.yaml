naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure ContentSafety Service — text:detectProtectedMaterial
  description: 'Microsoft Azure ContentSafety Service — text:detectProtectedMaterial. 1 operations. Lead operation: Microsoft
    Azure Analyze Protected Material. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - text:detectProtectedMaterial
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: contentsafety-service-text-detectprotectedmaterial
    baseUri: ''
    description: Microsoft Azure ContentSafety Service — text:detectProtectedMaterial business capability. Self-contained,
      no shared references.
    resources:
    - name: text:detectProtectedMaterial
      path: /text:detectProtectedMaterial
      operations:
      - name: microsoftazuretextoperationsdetecttextprotectedmaterial
        method: POST
        description: Microsoft Azure Analyze Protected Material
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: string
          description: The text protected material analysis request.
          required: true
  exposes:
  - type: rest
    namespace: contentsafety-service-text-detectprotectedmaterial-rest
    port: 8080
    description: REST adapter for Microsoft Azure ContentSafety Service — text:detectProtectedMaterial. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/text-detectprotectedmaterial
      name: text-detectprotectedmaterial
      description: REST surface for text:detectProtectedMaterial.
      operations:
      - method: POST
        name: microsoftazuretextoperationsdetecttextprotectedmaterial
        description: Microsoft Azure Analyze Protected Material
        call: contentsafety-service-text-detectprotectedmaterial.microsoftazuretextoperationsdetecttextprotectedmaterial
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: contentsafety-service-text-detectprotectedmaterial-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure ContentSafety Service — text:detectProtectedMaterial. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-analyze-protected-material
      description: Microsoft Azure Analyze Protected Material
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: contentsafety-service-text-detectprotectedmaterial.microsoftazuretextoperationsdetecttextprotectedmaterial
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
