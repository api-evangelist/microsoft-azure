naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure Document Intelligence (formerly Form Recognizer) — documentModels:compose
  description: 'Microsoft Azure Document Intelligence (formerly Form Recognizer) — documentModels:compose. 1 operations. Lead
    operation: Microsoft Azure Post Documentmodels:compose. Self-contained Naftiko capability covering one Microsoft Azure
    business surface.'
  tags:
  - Microsoft Azure
  - documentModels:compose
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: document-intelligence-formerly-form-recognizer-documentmodels-compose
    baseUri: ''
    description: Microsoft Azure Document Intelligence (formerly Form Recognizer) — documentModels:compose business capability.
      Self-contained, no shared references.
    resources:
    - name: documentModels:compose
      path: /documentModels:compose
      operations:
      - name: microsoftazuredocumentmodelscomposemodel
        method: POST
        description: Microsoft Azure Post Documentmodels:compose
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: composeRequest
          in: body
          type: string
          description: Compose request parameters.
          required: true
  exposes:
  - type: rest
    namespace: document-intelligence-formerly-form-recognizer-documentmodels-compose-rest
    port: 8080
    description: REST adapter for Microsoft Azure Document Intelligence (formerly Form Recognizer) — documentModels:compose.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/documentmodels-compose
      name: documentmodels-compose
      description: REST surface for documentModels:compose.
      operations:
      - method: POST
        name: microsoftazuredocumentmodelscomposemodel
        description: Microsoft Azure Post Documentmodels:compose
        call: document-intelligence-formerly-form-recognizer-documentmodels-compose.microsoftazuredocumentmodelscomposemodel
        with:
          composeRequest: rest.composeRequest
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: document-intelligence-formerly-form-recognizer-documentmodels-compose-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure Document Intelligence (formerly Form Recognizer) — documentModels:compose.
      One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-post-documentmodels-compose
      description: Microsoft Azure Post Documentmodels:compose
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: document-intelligence-formerly-form-recognizer-documentmodels-compose.microsoftazuredocumentmodelscomposemodel
      with:
        composeRequest: tools.composeRequest
      outputParameters:
      - type: object
        mapping: $.
