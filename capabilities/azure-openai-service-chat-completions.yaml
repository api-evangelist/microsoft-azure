naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure Azure OpenAI Service API — Chat Completions
  description: 'Microsoft Azure Azure OpenAI Service API — Chat Completions. 1 operations. Lead operation: Microsoft Azure
    Create a Chat Completion. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Chat Completions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: azure-openai-service-chat-completions
    baseUri: https://{endpoint}/openai
    description: Microsoft Azure Azure OpenAI Service API — Chat Completions business capability. Self-contained, no shared
      references.
    resources:
    - name: deployments-deployment-id-chat-completions
      path: /deployments/{deployment-id}/chat/completions
      operations:
      - name: chatcompletionscreate
        method: POST
        description: Microsoft Azure Create a Chat Completion
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_API_KEY}}'
  exposes:
  - type: rest
    namespace: azure-openai-service-chat-completions-rest
    port: 8080
    description: REST adapter for Microsoft Azure Azure OpenAI Service API — Chat Completions. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/deployments/{deployment-id}/chat/completions
      name: deployments-deployment-id-chat-completions
      description: REST surface for deployments-deployment-id-chat-completions.
      operations:
      - method: POST
        name: chatcompletionscreate
        description: Microsoft Azure Create a Chat Completion
        call: azure-openai-service-chat-completions.chatcompletionscreate
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: azure-openai-service-chat-completions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure Azure OpenAI Service API — Chat Completions. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-create-chat-completion
      description: Microsoft Azure Create a Chat Completion
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: azure-openai-service-chat-completions.chatcompletionscreate
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
