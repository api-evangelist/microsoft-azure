naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure CommunicationNetworkingClient — Turn
  description: 'Microsoft Azure CommunicationNetworkingClient — Turn. 1 operations. Lead operation: Microsoft Azure Issue
    A Configuration For An Stun Turn Server For An Existing Identity. Self-contained Naftiko capability covering one Microsoft
    Azure business surface.'
  tags:
  - Microsoft Azure
  - Turn
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: communicationnetworkingclient-turn
    baseUri: ''
    description: Microsoft Azure CommunicationNetworkingClient — Turn business capability. Self-contained, no shared references.
    resources:
    - name: networktraversal-:issueRelayConfiguration
      path: /networktraversal/:issueRelayConfiguration
      operations:
      - name: microsoftazurecommunicationnetworktraversalissuerelayconfiguration
        method: POST
        description: Microsoft Azure Issue A Configuration For An Stun Turn Server For An Existing Identity
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: string
  exposes:
  - type: rest
    namespace: communicationnetworkingclient-turn-rest
    port: 8080
    description: REST adapter for Microsoft Azure CommunicationNetworkingClient — Turn. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/networktraversal/issuerelayconfiguration
      name: networktraversal-issuerelayconfiguration
      description: REST surface for networktraversal-:issueRelayConfiguration.
      operations:
      - method: POST
        name: microsoftazurecommunicationnetworktraversalissuerelayconfiguration
        description: Microsoft Azure Issue A Configuration For An Stun Turn Server For An Existing Identity
        call: communicationnetworkingclient-turn.microsoftazurecommunicationnetworktraversalissuerelayconfiguration
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: communicationnetworkingclient-turn-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure CommunicationNetworkingClient — Turn. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-issue-configuration-stun
      description: Microsoft Azure Issue A Configuration For An Stun Turn Server For An Existing Identity
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: communicationnetworkingclient-turn.microsoftazurecommunicationnetworktraversalissuerelayconfiguration
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
