naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure AccessControlClient — SynapseRbacScopes
  description: 'Microsoft Azure AccessControlClient — SynapseRbacScopes. 1 operations. Lead operation: Microsoft Azure Get
    Rbacscopes. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - SynapseRbacScopes
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: accesscontrolclient-synapserbacscopes
    baseUri: ''
    description: Microsoft Azure AccessControlClient — SynapseRbacScopes business capability. Self-contained, no shared references.
    resources:
    - name: rbacScopes
      path: /rbacScopes
      operations:
      - name: microsoftazureroledefinitionslistscopes
        method: GET
        description: Microsoft Azure Get Rbacscopes
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: accesscontrolclient-synapserbacscopes-rest
    port: 8080
    description: REST adapter for Microsoft Azure AccessControlClient — SynapseRbacScopes. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/rbacscopes
      name: rbacscopes
      description: REST surface for rbacScopes.
      operations:
      - method: GET
        name: microsoftazureroledefinitionslistscopes
        description: Microsoft Azure Get Rbacscopes
        call: accesscontrolclient-synapserbacscopes.microsoftazureroledefinitionslistscopes
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: accesscontrolclient-synapserbacscopes-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure AccessControlClient — SynapseRbacScopes. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-get-rbacscopes
      description: Microsoft Azure Get Rbacscopes
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: accesscontrolclient-synapserbacscopes.microsoftazureroledefinitionslistscopes
      outputParameters:
      - type: object
        mapping: $.
