naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure CommunicationMessagesClient — Notifications
  description: 'Microsoft Azure CommunicationMessagesClient — Notifications. 1 operations. Lead operation: Microsoft Azure
    Post Messages Notifications:send. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Notifications
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: communicationmessagesclient-notifications
    baseUri: ''
    description: Microsoft Azure CommunicationMessagesClient — Notifications business capability. Self-contained, no shared
      references.
    resources:
    - name: messages-notifications:send
      path: /messages/notifications:send
      operations:
      - name: microsoftazurenotificationmessagesoperationssend
        method: POST
        description: Microsoft Azure Post Messages Notifications:send
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: string
          required: true
  exposes:
  - type: rest
    namespace: communicationmessagesclient-notifications-rest
    port: 8080
    description: REST adapter for Microsoft Azure CommunicationMessagesClient — Notifications. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/messages/notifications-send
      name: messages-notifications-send
      description: REST surface for messages-notifications:send.
      operations:
      - method: POST
        name: microsoftazurenotificationmessagesoperationssend
        description: Microsoft Azure Post Messages Notifications:send
        call: communicationmessagesclient-notifications.microsoftazurenotificationmessagesoperationssend
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: communicationmessagesclient-notifications-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure CommunicationMessagesClient — Notifications. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-post-messages-notifications-send
      description: Microsoft Azure Post Messages Notifications:send
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: communicationmessagesclient-notifications.microsoftazurenotificationmessagesoperationssend
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
