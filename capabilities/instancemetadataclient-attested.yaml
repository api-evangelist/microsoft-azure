naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure InstanceMetadataClient — Attested
  description: 'Microsoft Azure InstanceMetadataClient — Attested. 1 operations. Lead operation: Microsoft Azure Get Attested
    Document. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Attested
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: instancemetadataclient-attested
    baseUri: http://169.254.169.254/metadata
    description: Microsoft Azure InstanceMetadataClient — Attested business capability. Self-contained, no shared references.
    resources:
    - name: attested-document
      path: /attested/document
      operations:
      - name: microsoftazureattestedgetdocument
        method: GET
        description: Microsoft Azure Get Attested Document
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: nonce
          in: query
          type: string
          description: This is a string of up to 32 random alphanumeric characters.
  exposes:
  - type: rest
    namespace: instancemetadataclient-attested-rest
    port: 8080
    description: REST adapter for Microsoft Azure InstanceMetadataClient — Attested. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/attested/document
      name: attested-document
      description: REST surface for attested-document.
      operations:
      - method: GET
        name: microsoftazureattestedgetdocument
        description: Microsoft Azure Get Attested Document
        call: instancemetadataclient-attested.microsoftazureattestedgetdocument
        with:
          nonce: rest.nonce
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: instancemetadataclient-attested-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure InstanceMetadataClient — Attested. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-get-attested-document
      description: Microsoft Azure Get Attested Document
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: instancemetadataclient-attested.microsoftazureattestedgetdocument
      with:
        nonce: tools.nonce
      outputParameters:
      - type: object
        mapping: $.
