naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure DevSpacesManagement — Operations
  description: 'Microsoft Azure DevSpacesManagement — Operations. 1 operations. Lead operation: Microsoft Azure Lists Operations
    For The Resource Provider. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: devspacesmanagement-operations
    baseUri: https://management.azure.com
    description: Microsoft Azure DevSpacesManagement — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.DevSpaces-operations
      path: /providers/Microsoft.DevSpaces/operations
      operations:
      - name: microsoftazureoperationslist
        method: GET
        description: Microsoft Azure Lists Operations For The Resource Provider
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: devspacesmanagement-operations-rest
    port: 8080
    description: REST adapter for Microsoft Azure DevSpacesManagement — Operations. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-devspaces/operations
      name: providers-microsoft-devspaces-operations
      description: REST surface for providers-Microsoft.DevSpaces-operations.
      operations:
      - method: GET
        name: microsoftazureoperationslist
        description: Microsoft Azure Lists Operations For The Resource Provider
        call: devspacesmanagement-operations.microsoftazureoperationslist
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: devspacesmanagement-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure DevSpacesManagement — Operations. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-lists-operations-resource
      description: Microsoft Azure Lists Operations For The Resource Provider
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: devspacesmanagement-operations.microsoftazureoperationslist
      outputParameters:
      - type: object
        mapping: $.
