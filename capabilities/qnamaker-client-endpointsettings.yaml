naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure QnAMaker Client — endpointSettings
  description: 'Microsoft Azure QnAMaker Client — endpointSettings. 1 operations. Lead operation: Microsoft Azure Gets Endpoint
    Settings For An Endpoint. Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - endpointSettings
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: qnamaker-client-endpointsettings
    baseUri: ''
    description: Microsoft Azure QnAMaker Client — endpointSettings business capability. Self-contained, no shared references.
    resources:
    - name: endpointSettings
      path: /endpointSettings
      operations:
      - name: microsoftazureendpointsettingsgetsettings
        method: GET
        description: Microsoft Azure Gets Endpoint Settings For An Endpoint
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: qnamaker-client-endpointsettings-rest
    port: 8080
    description: REST adapter for Microsoft Azure QnAMaker Client — endpointSettings. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/endpointsettings
      name: endpointsettings
      description: REST surface for endpointSettings.
      operations:
      - method: GET
        name: microsoftazureendpointsettingsgetsettings
        description: Microsoft Azure Gets Endpoint Settings For An Endpoint
        call: qnamaker-client-endpointsettings.microsoftazureendpointsettingsgetsettings
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: qnamaker-client-endpointsettings-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure QnAMaker Client — endpointSettings. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-gets-endpoint-settings
      description: Microsoft Azure Gets Endpoint Settings For An Endpoint
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: qnamaker-client-endpointsettings.microsoftazureendpointsettingsgetsettings
      outputParameters:
      - type: object
        mapping: $.
