naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure DomainRegistrationProvider API Client — DomainRegistrationProvider
  description: 'Microsoft Azure DomainRegistrationProvider API Client — DomainRegistrationProvider. 1 operations. Lead operation:
    Microsoft Azure Implements Csm Operations Api To Exposes The List Of Available Csm Apis Under The Resource Provider. Self-contained
    Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - DomainRegistrationProvider
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: domainregistrationprovider-api-client-domainregistrationprovider
    baseUri: https://management.azure.com
    description: Microsoft Azure DomainRegistrationProvider API Client — DomainRegistrationProvider business capability. Self-contained,
      no shared references.
    resources:
    - name: providers-Microsoft.DomainRegistration-operations
      path: /providers/Microsoft.DomainRegistration/operations
      operations:
      - name: microsoftazuredomainregistrationproviderlistoperations
        method: GET
        description: Microsoft Azure Implements Csm Operations Api To Exposes The List Of Available Csm Apis Under The Resource
          Provider
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: domainregistrationprovider-api-client-domainregistrationprovider-rest
    port: 8080
    description: REST adapter for Microsoft Azure DomainRegistrationProvider API Client — DomainRegistrationProvider. One
      Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-domainregistration/operations
      name: providers-microsoft-domainregistration-operations
      description: REST surface for providers-Microsoft.DomainRegistration-operations.
      operations:
      - method: GET
        name: microsoftazuredomainregistrationproviderlistoperations
        description: Microsoft Azure Implements Csm Operations Api To Exposes The List Of Available Csm Apis Under The Resource
          Provider
        call: domainregistrationprovider-api-client-domainregistrationprovider.microsoftazuredomainregistrationproviderlistoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: domainregistrationprovider-api-client-domainregistrationprovider-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure DomainRegistrationProvider API Client — DomainRegistrationProvider. One tool
      per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-implements-csm-operations
      description: Microsoft Azure Implements Csm Operations Api To Exposes The List Of Available Csm Apis Under The Resource
        Provider
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: domainregistrationprovider-api-client-domainregistrationprovider.microsoftazuredomainregistrationproviderlistoperations
      outputParameters:
      - type: object
        mapping: $.
