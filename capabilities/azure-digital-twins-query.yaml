naftiko: 1.0.0-alpha2
info:
  label: Microsoft Azure Azure Digital Twins API — Query
  description: 'Microsoft Azure Azure Digital Twins API — Query. 1 operations. Lead operation: Microsoft Azure Post Query.
    Self-contained Naftiko capability covering one Microsoft Azure business surface.'
  tags:
  - Microsoft Azure
  - Query
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_API_KEY: MICROSOFT_AZURE_API_KEY
capability:
  consumes:
  - type: http
    namespace: azure-digital-twins-query
    baseUri: https://digitaltwins-hostname
    description: Microsoft Azure Azure Digital Twins API — Query business capability. Self-contained, no shared references.
    resources:
    - name: query
      path: /query
      operations:
      - name: microsoftazurequeryquerytwins
        method: POST
        description: Microsoft Azure Post Query
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: querySpecification
          in: body
          type: string
          description: The query specification to execute.
          required: true
  exposes:
  - type: rest
    namespace: azure-digital-twins-query-rest
    port: 8080
    description: REST adapter for Microsoft Azure Azure Digital Twins API — Query. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/query
      name: query
      description: REST surface for query.
      operations:
      - method: POST
        name: microsoftazurequeryquerytwins
        description: Microsoft Azure Post Query
        call: azure-digital-twins-query.microsoftazurequeryquerytwins
        with:
          querySpecification: rest.querySpecification
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: azure-digital-twins-query-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Azure Azure Digital Twins API — Query. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: microsoft-azure-post-query
      description: Microsoft Azure Post Query
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: azure-digital-twins-query.microsoftazurequeryquerytwins
      with:
        querySpecification: tools.querySpecification
      outputParameters:
      - type: object
        mapping: $.
