swagger: '2.0'
info:
  version: 2020-08-01-preview
  title: Microsoft Azure AccessControlClient AccessConnector SynapseRbacScopes API
schemes:
- https
tags:
- name: SynapseRbacScopes
paths:
  /rbacScopes:
    get:
      tags:
      - SynapseRbacScopes
      operationId: microsoftAzureRoledefinitionsListscopes
      description: List rbac scopes.
      x-ms-examples:
        List rbac scopes:
          $ref: ./examples/RoleDefinitions_ListScopes.json
      consumes: []
      produces:
      - application/json
      - text/json
      parameters:
      - $ref: '#/parameters/ApiVersionParameter'
      responses:
        '200':
          description: Success response.
          schema:
            description: A list of Synapse rbac scopes available.
            type: array
            items:
              description: Synapse rbac scope.
              type: string
        default:
          description: Error response describing why the operation failed.
          schema:
            $ref: ./roleAssignments.json#/definitions/ErrorContract
      summary: Microsoft Azure Get Rbacscopes
parameters:
  ApiVersionParameter:
    name: api-version
    in: query
    required: true
    type: string
    description: The Synapse client API Version.
x-ms-parameterized-host:
  hostTemplate: '{endpoint}'
  useSchemePrefix: false
  parameters:
  - $ref: '#/parameters/Endpoint'
