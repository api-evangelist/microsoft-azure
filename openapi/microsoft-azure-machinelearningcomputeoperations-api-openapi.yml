openapi: 3.2.0
info:
  version: 2020-08-01-preview
  title: Microsoft Azure AccessControlClient AccessConnector Machine Learning Compute Operations API
tags:
- name: MachineLearningComputeOperations
paths:
  /providers/Microsoft.MachineLearningCompute/operations:
    get:
      tags:
      - MachineLearningComputeOperations
      operationId: microsoftAzureMachinelearningcomputeListavailableoperations
      x-ms-examples:
        Machine Learning Compute List Available Operations:
          $ref: examples/MachineLearningCompute_ListAvailableOperations.json
      description: Gets all available operations.
      parameters:
      - $ref: '#/components/parameters/ApiVersionParameter'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AvailableOperations'
      summary: Microsoft Azure Get Providers Microsoft Machinelearningcompute Operations
components:
  schemas:
    ResourceOperation:
      type: object
      description: Resource operation
      properties:
        name:
          type: string
          description: Name of this operation.
        display:
          type: object
          description: Display of the operation
          properties:
            provider:
              type: string
              description: The resource provider name
            resource:
              type: string
              description: The resource name
            operation:
              type: string
              description: The operation
            description:
              type: string
              description: The description of the operation
        origin:
          type: string
          description: The operation origin
    AvailableOperations:
      type: object
      description: Available operation list
      properties:
        value:
          type: array
          description: An array of available operations
          items:
            $ref: '#/components/schemas/ResourceOperation'
  parameters:
    ApiVersionParameter:
      in: query
      name: api-version
      description: The version of the Microsoft.MachineLearningCompute resource provider API to use.
      required: true
      schema:
        type: string
x-ms-parameterized-host:
  hostTemplate: '{endpoint}'
  useSchemePrefix: false
  parameters:
  - $ref: '#/components/parameters/Endpoint'
